/**
 * Identifiant de build exposé au client et injecté dans le Service Worker.
 * Généré automatiquement dans `next.config.ts` (git SHA + horodatage process).
 */
export const APP_BUILD_ID =
  process.env.NEXT_PUBLIC_APP_BUILD_ID ?? "development";

/** Version package affichée dans le Profil (SemVer produit). */
export const APP_PACKAGE_VERSION = "0.1.0";
